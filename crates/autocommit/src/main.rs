//! autocommit — offline scoped Conventional Commits message generator.
//!
//! This is a bootstrap placeholder binary created while standing up the
//! repository standard. The real command-line pipeline (staged diff reading,
//! deterministic type/scope classification, local LLM description drafting and
//! Conventional Commits validation) lands in Phase 1.

fn main() {
    println!("autocommit: bootstrap placeholder. The CLI is implemented in Phase 1.");
}
