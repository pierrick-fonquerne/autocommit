//! autocommit library: the engine behind the `autocommit` CLI.
//!
//! Exposes the building blocks of the staged-diff-to-Conventional-Commit
//! pipeline. Phase 1 starts with configuration loading.

pub mod config;
